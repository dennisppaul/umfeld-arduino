@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# If you need to propagate any find_package dependencies to consumers, add them here, e.g.:
# find_dependency(SDL3 REQUIRED)
# find_dependency(Freetype REQUIRED)
# Usually not needed because the exported targets carry transitive linkage.

# Load the exported targets for this package
include("${CMAKE_CURRENT_LIST_DIR}/UmfeldTargets.cmake")

# Provide a convenient imported alias if not already present
if(NOT TARGET Umfeld::Umfeld)
  add_library(Umfeld::Umfeld INTERFACE IMPORTED)
  set_property(TARGET Umfeld::Umfeld PROPERTY INTERFACE_LINK_LIBRARIES Umfeld)
endif()
